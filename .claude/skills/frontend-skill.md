# Skill: frontend — Rideglory

> Generated by: /solo-approve (domain) + Architect phase iter 1 (technical)
> Last updated: —
> Phase: not yet generated

---

## Domain context
<!-- Populated by /solo-approve from the approved PRD + PLAN.md -->

## Scope boundaries
<!-- What is explicitly IN and OUT of scope — populated by /solo-approve -->

## Key acceptance patterns
<!-- Recurring acceptance criteria patterns — populated by /solo-approve -->

## [Technical context — populated by Architect]
<!-- Flutter stack: Clean Architecture, BLoC/Cubit, ResultState<T>, Retrofit, Firebase Auth -->
<!-- rideglory-api: NestJS microservices, Firebase ID token validation -->
<!-- Commands: dart analyze, flutter test, dart run build_runner build --delete-conflicting-outputs -->

## Gotchas and learnings
<!-- Things discovered during execution that future iterations should know -->

## Change log
<!-- Append, never delete -->
- 2026-05-11 (iter 0): Skill stub created. Run /solo-plan then /solo-approve to populate.
- 2026-05-12 (iter 2): EventService @Query params + EventsCubit _fetchFn signature upgrade to accept {type, dateFrom, dateTo, city}; updateFilters/clearFilters now call fetchEvents() for backend refetch; filter badge upgraded to count (int) with 16×16 circle; conditional "Limpiar filtros" in bottom sheet; filtered vs all-events empty state in EventsPageView. GetUserByIdUseCase + UserService.getUserById + UserRepository.getUserById + UserRepositoryImpl. RiderProfileCubit (@injectable) + RiderProfilePage (4 ResultState branches, BlocProvider+getIt) + RiderProfileContent + RiderProfileLoading. AppRoutes.riderProfile + GoRoute registered. Attendee list tap → pushNamed(riderProfile, extra: userId). Chevron affordance on AttendeeProcessedItem when onTap present. 8 l10n keys added to ARB + app_localizations + app_localizations_es. build_runner clean (118 outputs). ADR-3 compliance: no EventsState freezed refactor.

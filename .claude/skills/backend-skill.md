# Skill: backend — Rideglory

> Generated by: /solo-approve (domain)
> Last updated: 2026-05-12 — Iteration 0 (plan approval)
> Phase: domain

---

## Domain context

The backend is **rideglory-api** at `/Users/cami/Developer/Personal/rideglory-api` — a NestJS microservices system with an api-gateway and independent microservices (events-ms, vehicles-ms, users-ms, maintenances-ms). All HTTP routes are protected by Firebase Auth guard; the gateway validates Firebase ID tokens and forwards requests to microservices via a message bus.

**Domain business rules:**
- A rider must be authenticated to access any resource (no public endpoints except auth).
- An event organizer must own the event to approve/reject registrations, change event status, or delete it.
- Vehicle documents (SOAT, future: tecno) are associated to a `(userId, vehicleId)` pair.
- FCM device tokens are associated to `userId` in a `device_tokens` table; one user may have multiple tokens (multiple devices). Send to all tokens for that user.
- SOS alerts are broadcast to all WebSocket clients in the same `eventId` session — not via FCM.
- Event status transitions drive FCM sends: `pending → in_progress` notifies all approved registrants; `any → cancelled` notifies all registrants of any status.

---

## Scope boundaries

**New backend work per approved plan:**

| Iter | Backend work |
|------|-------------|
| 2 | Wire filter query params (type, date, city) from api-gateway through message bus to events-ms |
| 3a | New `insurance` module: Prisma schema for `InsuranceDocument`, endpoints POST/GET/PATCH, Claude Haiku PDF extraction service, Firebase Storage Admin SDK reader |
| 4 | New `POST /events/generate-cover` endpoint: Claude Haiku generates search query → Unsplash API returns image URL |
| 5 | New `GET /events/recommendations` endpoint: scoring algorithm (vehicle type match, past registrations, proximity) |
| 6a | New `notifications` module: FCM token registration `POST /users/device-token`, Firebase Admin SDK send, handlers for all 5 FCM trigger events |
| 6b | WebSocket SOS message type: `{ type: "sos", riderId, lat, lng, eventId }` — gateway broadcasts to all clients in same eventId session |

**Out of scope for backend:**
- No FCM for SOS (WebSocket only)
- No ML model for recommendations (deterministic scoring only)
- No organizer SOS dismiss endpoint in v1
- No public URLs for Firebase Storage PDF — Admin SDK path-based access only

---

## Key acceptance patterns

Every backend change must:
1. Pass `nest build` with zero TypeScript errors.
2. All protected routes have `@UseGuards(FirebaseAuthGuard)` — no endpoint exposed unauthenticated.
3. Firebase Storage PDF access uses Admin SDK `bucket.file(path).download()` — never a public URL.
4. FCM payloads: scalar IDs only (`eventId`, `registrationId`, `route`) — no nested objects.
5. Claude API calls use `claude-haiku-4-5` model (cost-efficient for extraction tasks).
6. New Prisma models must have a migration file; never modify prod schema without a migration.

**API contract conventions:**
- `POST` bodies: validated with NestJS `class-validator` DTOs.
- Error responses: `{ message: string, statusCode: number, error: string }`.
- Firebase Storage paths passed TO backend: `insurance/{userId}/{vehicleId}/soat.pdf`.
- Backend returns `storageUrl` (download URL with short TTL) for frontend display.

---

## New API contracts (from plan)

### SOAT (Iter 3a)
```
POST /vehicles/:vehicleId/insurance
Body: { docType: 'soat' | 'tecno' | 'other', storagePath: string }
Response: { id, vehicleId, docType, storagePath, expirationDate, extractionConfidence, status }

GET /vehicles/:vehicleId/insurance
Response: InsuranceDocument[]

PATCH /vehicles/:vehicleId/insurance/:id
Body: { expirationDate: string } // manual override
Response: InsuranceDocument
```

### AI Cover (Iter 4)
```
POST /events/generate-cover
Body: { title: string, type: string, location: string }
Response: { imageUrl: string, query: string }
```

### Recommendations (Iter 5)
```
GET /events/recommendations
Headers: Authorization: Bearer <firebase-id-token>
Response: EventDto[] (max 5)
```

### FCM Token (Iter 6a)
```
POST /users/device-token
Body: { token: string, platform: 'android' | 'ios' }
Response: { success: boolean }
```

### SOS WebSocket message (Iter 6b)
```json
{ "type": "sos", "riderId": "...", "riderName": "...", "lat": 0.0, "lng": 0.0, "eventId": "...", "active": true }
```

---

## [Technical context — populated by Architect]

Stack: NestJS, Prisma ORM, Firebase Admin SDK, Anthropic Node.js SDK, Unsplash API, Firebase Cloud Messaging.
Repo: `/Users/cami/Developer/Personal/rideglory-api`

---

## Gotchas and learnings

<!-- Append during iterations -->

---

## Change log

- 2026-05-11 (iter 0): Skill stub created.
- 2026-05-12 (iter 0): Domain content populated from approved PRD + PLAN.md via /solo-approve.
- 2026-05-13 (iter-4): POST /events/generate-cover implemented. ClaudeService (Anthropic SDK, claude-haiku-4-5) + UnsplashService (axios, 15 s timeout, typed response interface) created in api-gateway/src/common/. GenerateCoverDto with class-validator. Both services registered in EventsModule. .env.example created with ANTHROPIC_API_KEY + UNSPLASH_ACCESS_KEY placeholders. 10 unit tests: all pass. 0 lint errors in new code. Pattern: services in common/, DTO in events/dto/, register in EventsModule providers array.
- 2026-05-15 (iter-3): Tracking organizer controls + SOS + route GeoJSON + geocode proxy + cron reminders implemented. Key patterns: (1) Organizer guard is a synchronous ownerId check after findOne — no separate guard class needed. (2) SOS dedup: check `sosTriggeredAt === null` in events-ms then set it atomically — returns `triggered: boolean` for gateway decision. (3) Prisma JSON? field stores GeoJSON; returned as plain JS object from service (cast to `object`). (4) Mapbox geocode proxy uses global `fetch` — no axios; encodeURIComponent for address. (5) @Cron expressions for once-daily reminders use `'0 9 * * *'`; for every-15-min use `'*/15 * * * *'`; both with `timeZone: 'America/Bogota'`. (6) FCM multicast: fetch all approved registrant userIds from events-ms, then loop over users-ms to get fcmToken per user — two sequential RPC calls per user, non-fatal if any fails. (7) TrackingModule now imports NotificationsModule and USERS_SERVICE for SOS FCM multicast. (8) `nest build` includes prisma/seed.ts in compilation — keep seed fields in sync with schema or it fails to build. (9) `prisma migrate reset` is blocked by Prisma AI safety guard — create SQL migration files manually and apply with `prisma db push` or `migrate deploy`.

---
## Plan reapproval update — 2026-05-13 (plan v3, iters 1–5)

### Backend scope by iteration

**Iter-1 (Redesign):** Zero backend changes. Skip backend phase entirely.

**Iter-2 (SOAT + Notifications):**
- vehicles-ms: Soat entity (Prisma), POST/GET /api/vehicles/:vehicleId/soat
- users-ms: fcmToken String? field on User, POST /api/notifications/fcm-token
- api-gateway (FIRST-TIME Prisma): Notification entity (id, userId, type, payload JSON, isRead, createdAt), GET /api/notifications (cursor), PATCH /api/notifications/:id/read, PATCH /api/notifications/read-all
- api-gateway: @nestjs/schedule + NotificationSchedulerService; SOAT cron (30d, 7d, day-of); America/Bogota timezone
- events-ms: FCM push trigger on registration approval/rejection; push inserts row in notifications table

**Iter-3 (Tracking):**
- api-gateway/tracking: POST /api/events/:eventId/tracking/start, POST /api/events/:eventId/tracking/end (organizer-level — different from existing rider session join)
- TrackingGateway: handle 'sos' WebSocket message → broadcast sos_alert + FCM multicast; deduplication via sosTriggeredAt
- events-ms: GET /api/events/:eventId/route returning routeGeoJson (GeoJSON LineString, NOT encoded polyline); schedule 24h event reminder

**Iter-4 (Followers):**
- users-ms: Follow entity (followerId, followingId, createdAt, composite unique index); POST/DELETE /api/users/:userId/follow; GET /api/users/:userId/followers?page=&limit=; GET /api/users/:userId/following?page=&limit=
- users-ms: _count.followers and _count.following in profile response; GET /api/vehicles?userId=:userId returning PublicVehicleDto (no licensePlate or insurance fields)
- api-gateway: FCM push for nueva_suscripcion; row in notifications table

**Iter-5 (Deep Links):**
- events-ms: GET /api/events/:eventId/share-metadata with Cache-Control: public, max-age=3600
- Firebase Hosting or api-gateway: /.well-known/assetlinks.json, /.well-known/apple-app-site-association, store redirect page

### Security rules (all iters)
- Every protected endpoint: Firebase ID token guard (FirebaseAuthGuard)
- No API keys in source code
- FCM tokens stored per-user, updated on each login

### Pre-flight for iter-2 (critical order)
1. Create seed.ts in vehicles-ms + events-ms
2. prisma migrate reset in 4 existing services
3. prisma init + prisma migrate dev in api-gateway (first-time)
4. Verify GET /api/vehicles returns 200 empty

## Change log
- 2026-05-13 (plan v3 approval): Backend scope documented per iteration. Pre-flight order for iter-2 established.

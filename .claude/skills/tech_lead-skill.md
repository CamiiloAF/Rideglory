# Skill: tech_lead — Rideglory

> Generated by: /solo-approve (domain)
> Last updated: 2026-05-12 — Iteration 0 (plan approval)
> Phase: domain

---

## Domain context

Rideglory is a brownfield Flutter mobile app. The tech lead role reviews code for architectural correctness, security compliance, quality gate conformance, and rideglory-coding-standards adherence. The tech lead does NOT implement features — it reviews diffs and produces feedback or sign-off.

The tech lead also owns the code review and optimization story (HU-REFACTOR-01) in Iteration 1.

---

## Scope boundaries

Tech lead reviews are triggered after the frontend agent completes each iteration. The review covers:
- Clean Architecture layer compliance
- DI wiring correctness
- State management patterns
- Localization completeness
- Security constraints
- Code quality (style, dead code, linting)

Tech lead is NOT responsible for:
- Writing tests (QA role)
- Designing UI (design role)
- Backend code (backend role)

---

## Security checklist (review every iteration)

1. No secrets or credentials committed to source — `.env.example` has placeholders only.
2. Firebase config files (`google-services.json`, `GoogleService-Info.plist`) not tracked in git.
3. Firebase ID tokens are NOT logged or stored locally in plain text.
4. No `BuildContext` in data layer files — cross-check `lib/features/*/data/` for `import 'package:flutter/...'`.
5. No `print()` calls in `lib/` — use proper logging or remove.
6. No SQL / NoSQL injection vectors (Firestore queries use typed SDK, not raw strings).
7. No hardcoded API keys or tokens in source code.

---

## Architecture review checklist

### Layer compliance
- Domain layer (`lib/features/*/domain/`): no Flutter imports, no HTTP calls, no `dart:io`
- Data layer (`lib/features/*/data/`): no widgets, no `BuildContext`
- Presentation layer (`lib/features/*/presentation/`): no direct HTTP calls, no DTO exposure (uses domain models only)

### DI review
- New `@injectable` / `@lazySingleton` classes registered in `injection.dart`
- New global cubits added to root `MultiBlocProvider` in `main.dart`
- `dart run build_runner build` succeeds without conflicts after DI changes

### State management
- All async state uses `ResultState<T>` — no boolean loading/error flags
- Complex state (2+ independent results) uses a `@freezed` state class
- Cubits never expose DTOs — always convert to domain models before emitting

### Naming and style
- Variables use domain names (not single-letter generics)
- Button text: sentence case (`'Iniciar sesión'`, not `'INICIAR SESIÓN'`)
- One widget per file
- No obvious comments (self-explanatory identifiers only)

### Localization
- All user-visible strings in `app_es.arb` with feature prefix
- `context.l10n.<key>` in widgets — never string literals
- New ARB keys follow naming convention: `<feature>_<descriptiveName>`

### Code quality (from HU-REFACTOR-01)
Items the tech lead identifies and flags for the frontend agent to fix:
- `print()` calls in `lib/` → remove or replace
- Unused imports → remove
- Unused variables / dead code → remove
- `// TODO` and `// FIXME` comments → resolve or convert to GitHub issue
- Raw Material widgets (`ElevatedButton`, `TextField`, `TextButton`) where shared equivalents exist
- Missing `const` constructors on widgets that can be const
- `BuildContext` stored across async gaps without `mounted` check

---

## Success criteria → reviewable properties

| PRD success criterion | Reviewable check |
|----------------------|-----------------|
| `dart analyze` zero violations | Run `dart analyze --no-summary` and count issues |
| `flutter test` green | Run `flutter test` and check exit code |
| No hardcoded strings | `grep -r '"[A-Za-záéíóúñ ]' lib/features/` — any match without `l10n` context is a violation |
| No layer violations | `grep -r "import 'package:flutter" lib/features/*/domain/` and `lib/features/*/data/` |
| No raw Material widgets | `grep -r "ElevatedButton\|TextField\(\|TextButton" lib/features/` |
| No print() | `grep -rn "print(" lib/` |

---

## Risks flagged for extra scrutiny (from plan)

| Risk | What to watch for |
|------|-----------------|
| EventFormCubit state refactor (Iter 4) | Ensure existing EventFormCubit tests still pass after freezed conversion; check for DTO leakage |
| FCM deep-link routing (Iter 6a) | Verify `NotificationNavigator` pattern; no `go_router extra` in FCM payloads |
| Claude Haiku extraction reliability (Iter 3a/3b) | Confirmation step must always show before saving; low-confidence warning must be visible |
| Firebase Storage PDF access (Iter 3a) | Backend must use Admin SDK path-based read; no public URL stored or passed |
| SOS overlay covering map (Iter 6b) | Overlay must be top-anchor banner; map widget must remain hittable below it |

---

## Code review output format (HU-REFACTOR-01 deliverable)

`docs/architecture/code-review-iter1.md` must contain:

```markdown
# Code Review — Iteration 1

> Reviewer: tech_lead
> Date: <date>

## Findings

| File | Issue | Resolution | Deferred? |
|------|-------|-----------|-----------|

## Deferred items
<items not fixed in this iteration + rationale>

## Architecture health summary
<3-5 bullet assessment of overall code health>
```

---

## [Technical context — populated by Architect]

Stack: Flutter Clean Architecture + BLoC/Cubit + ResultState<T> + Retrofit + Firebase.
Commands: `dart analyze --no-summary`, `grep -rn "print(" lib/`, `dart fix --apply`.

---

## Gotchas and learnings

### Iteration 1 (2026-05-12)

- **One-widget-per-file is strictly enforced for private widgets too.** `_ProfileContent` inside `profile_page.dart` is a violation even if it's never referenced externally. Extract every secondary widget to its own file.
- **`print()` inside `kDebugMode` guards still violates US-1-5.** Replace with `log()` from `dart:developer`.
- **`TextButton` is blocked even with custom padding override.** Use `AppTextButton(visualDensity: VisualDensity.compact)` instead.
- **`OutlinedButton.icon` is acceptable when `AppButton` can't cover icon + dynamic text label** (e.g., date-picker triggers). Document rationale in-code with a comment.
- **`dart analyze` in this project uses `--fatal-warnings` by default** — the `exit code 2` from analyze means warnings were found. Target is zero warnings, not just zero errors.
- **`dart analyze --no-summary` does not exist in this version of the Dart SDK.** Use `dart analyze` directly.
- **`flutter gen-l10n` can run standalone** even when `build_runner` is broken — ARB keys can be added and localization regenerated independently.
- **Pre-existing `withOpacity` deprecations are info-level** and should be batched in a dedicated cleanup iteration, not fixed one by one during feature reviews (too much churn in shared widgets).

---

## Change log

- 2026-05-11 (iter 0): Skill stub created.
- 2026-05-12 (iter 0): Domain content populated from approved PRD + PLAN.md via /solo-approve.
- 2026-05-12 (iter 1): Gotchas and learnings appended after PR #8 review. 6 blocking issues fixed.
- 2026-05-13 (iter 4): PR #11 reviewed and approved. 1 blocking fix: prefer_const_constructors in test file (const Right, Left, DomainException). dart analyze 34 pre-existing items only. flutter test 7/7. code-review-iter4.md produced.

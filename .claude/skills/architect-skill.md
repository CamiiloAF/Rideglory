# Skill: architect — Rideglory

> Generated by: /solo-approve (domain)
> Last updated: 2026-05-12 — Iteration 0 (plan approval)
> Phase: domain

---

## Domain context

Rideglory is a brownfield Flutter mobile app (Android + iOS) with a NestJS microservices backend. The stack is fixed — no stack selection. The architect role validates existing choices, identifies new libraries/patterns needed per iteration, and defines API contracts for new features.

Personas: **Rider** (end-user) and **Event Organizer** (admin-lite). All flows are mobile-first (375px, touch targets ≥44×44px).

---

## Scope boundaries

**Architecture is constrained by:**
- Flutter (Android API 21 min, iOS 13 min) — no web target
- Clean Architecture strictly: domain → data ← presentation; dependencies flow inward
- BLoC/Cubit + ResultState<T> for all async state — never boolean loading flags
- Firebase Auth ID tokens on every rideglory-api request
- All strings Spanish, in ARB

**New integrations per approved plan:**
- Iter 3a: Claude Haiku API (PDF text extraction for SOAT), Firebase Storage Admin SDK (backend reads PDFs by path)
- Iter 4: Unsplash API (image search) + Claude Haiku (search query generation)
- Iter 5: Backend scoring algorithm for recommendations (no external ML)
- Iter 6a: Firebase Cloud Messaging (FCM) — `firebase_messaging` Flutter + Firebase Admin SDK for backend send
- Iter 6b: WebSocket SOS message type extension on existing `TrackingWsClient`

---

## Key acceptance patterns

Quality gates that architect validates before closing an iteration:
1. No new `dart analyze` violations introduced
2. Code generation (`dart run build_runner build`) succeeds with no conflicts
3. No `BuildContext` in data layer files
4. DI: new services/cubits properly registered in `injection.dart`; new global cubits added to root `MultiBlocProvider`
5. New API contracts match DTOs exactly — no field name drift between Flutter DTO and NestJS response

**API contract conventions:**
- All endpoints: Firebase Auth ID token required (JWT bearer via `FirebaseAuthInterceptor`)
- Error responses: consistent `{ message, statusCode, error }` shape
- Firebase Storage paths (not public URLs) passed to backend — backend reads via Admin SDK
- FCM payloads carry only scalar IDs (`eventId`, `registrationId`) — never `extra` objects; target screens must have "by-id" route variants

---

## Architectural risks (from plan)

| Risk | Mitigation | Hits |
|------|-----------|------|
| Event filter params not wired to events-ms | Backend work in-scope for Iter 2 | Iter 2 |
| go_router extra non-serializable from FCM | NotificationNavigator + by-id routes | Iter 6a |
| Claude Haiku PDF extraction accuracy | Confirmation step + manual fallback | Iter 3a |
| FCM iOS APNs setup | Confirm APNs key + Xcode capability before 6a | Iter 6a |
| Firebase Storage PDF access from backend | Admin SDK path-based read, not public URL | Iter 3a |
| EventFormCubit refactor regression (Iter 4) | Update existing tests after freezed conversion | Iter 4 |

---

## ADRs locked in plan

| ADR | Decision |
|-----|---------|
| ADR-1 | Mock library: `mocktail` (no code-gen, compatible with abstract class patterns) |
| ADR-2 | Profile photo deferred — profilePhotoUrl not in Prisma schema for v1 |
| ADR-3 | Firebase Storage PDF path → backend Admin SDK (not public URL fetch) |
| ADR-4 | AI covers: Claude Haiku generates 3–5 word query → Unsplash API returns photo (not image generation) |
| ADR-5 | FCM deep-link: scalar IDs in payload + NotificationNavigator; go_router extra avoided |
| ADR-6 | SOS organizer dismiss: out of scope v1; only sender can cancel |

---

## [Technical context — populated by Architect per iteration]

Stack: Flutter + Firebase (Auth, Storage, Remote Config) + rideglory-api NestJS + Dio/Retrofit + BLoC/Cubit + go_router + GetIt/Injectable + freezed/json_serializable + google_maps_flutter + web_socket_channel + shared_preferences + image_picker + permission_handler.

Commands: `dart analyze`, `flutter test`, `dart run build_runner build --delete-conflicting-outputs`, `flutter pub get`.

---

## Gotchas and learnings

<!-- Append during iterations -->
- 2026-05-12 (iter-1): Iteration 1 is intentionally backend-free. Architect handoff is light — main work is confirming `GET /users/me` shape and writing the `ProfileCubit` DI ADR. Always check `lib/features/<feature>/` for existing service/repository before declaring a new endpoint.
- 2026-05-12 (iter-1): `UserModel` already exists with rich rider profile fields (eps, bloodType, emergency contacts). No need to add fields for the profile page — current model is over-specified, not under.
- 2026-05-12 (iter-1): Stub `profile_page.dart` is a `StatelessWidget` with only `ListTile` actions; full rewrite needed for header + main vehicle + ResultState branches. Frontend should keep existing logout + my-registrations actions when rewriting.
- 2026-05-12 (iter-1): Reusable initials helper belongs in `lib/core/utils/` not in the profile feature — Iteration 2 attendee list will reuse it.

---

## Change log

- 2026-05-11 (iter 0): Skill stub created.
- 2026-05-12 (iter 0): Domain content populated from approved PRD + PLAN.md via /solo-approve.
- 2026-05-12 (iter-1): Architect phase complete. Confirmed no backend changes. Defined ProfileCubit DI scope (ADR-1, lazySingleton in root MultiBlocProvider). Confirmed no photo upload v1 (ADR-2). New artifacts: GetMyProfileUseCase + ProfileCubit + initials helper. l10n keys with `profile_` prefix. DIAGRAMS.md initialized with profile fetch sequence.

import 'package:flutter/cupertino.dart';

abstract class AppDimens {
  static const matchPageHorizontalMargin = EdgeInsets.symmetric(horizontal: 20);
  static final matchCardBorderRadius = BorderRadius.circular(16.0);
  static const inputsMargin = EdgeInsets.symmetric(vertical: 12);
  static const inputsContentPadding =
      EdgeInsets.symmetric(vertical: 16, horizontal: 8);
}

import 'package:go_router/go_router.dart';

/// Rutas de `live_ride` (Bloque 3, F11). Vacías por ahora: el diseño de
/// las pantallas (LV1-LV6) está en marcha en Pencil (F9) y otro agente
/// registrará aquí `GoRoute`s para `AppRoutes.eventLive`,
/// `AppRoutes.eventLiveSos` y `AppRoutes.eventLiveRiders` una vez
/// aprobadas. Se registra desde ya en `app_router.dart` para que la línea
/// de integración no quede pendiente para esa fase.
List<RouteBase> liveRideRoutes = [];

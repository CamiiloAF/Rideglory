# ENV Delta — rtm-backend-persistencia

**Date:** 2026-06-04T17:32:49Z

## Delta

**Ninguna variable de entorno nueva requerida.**

La implementación de RTM usa `DATABASE_URL` que ya existe en `vehicles-ms/.env` (o variable de entorno del contenedor).

No se añaden servicios externos, no se introducen nuevas URLs de terceros.

# Review Checklist — prd-maintenance

> Generated by PO close-out phase. Human reviews this before committing.

## Pre-commit steps (backend)

- [ ] `cd /Users/cami/Developer/Personal/rideglory-api/maintenances-ms`
- [ ] `npx prisma migrate dev --name maintenance_mode_workshop_fields`
- [ ] `npx prisma generate`
- [ ] Restart maintenances-ms; verify it boots cleanly

## Pre-commit steps (Flutter)

- [ ] `dart run build_runner build --delete-conflicting-outputs` (for `create_maintenance_response_dto.dart` — needs `.g.dart` generated)
- [ ] `dart analyze` — expect 0 errors
- [ ] `flutter test` — expect 34 pass; 3 pre-existing failures (EventType.onRoad) acceptable

## Manual smoke tests

- [ ] Create a **completed** maintenance with next km interval → list shows 2 records (1 completed + 1 scheduled)
- [ ] Create a **scheduled** maintenance (no service date) → appears in PRÓXIMAMENTE or ATRASADO section based on dates
- [ ] Garage widget: left card = last completed maintenance (serviceDate + odometerAtService); right card = next scheduled (nextDate + nextOdometer) — from different records
- [ ] Status filter "Atrasado" → only overdue scheduled records visible
- [ ] Status filter "Próximo" → only upcoming-within-threshold scheduled records visible
- [ ] Status filter "Al día" → upToDate scheduled + completed records visible
- [ ] Status filter "Todos" → all records visible
- [ ] Detail screen — completed maintenance: shows serviceDate, odometerAtService, workshop; no NEXT SERVICE card unless next fields filled
- [ ] Detail screen — scheduled maintenance: shows NEXT SERVICE card with nextDate / nextOdometer
- [ ] Old records (if any in dev DB) display via legacy fallback — no crashes

## AC sign-off

| AC | Description | Signed off |
|----|-------------|-----------|
| AC-1 | MaintenanceModel new fields | [ ] |
| AC-2 | Status calc: overdue/next/upToDate | [ ] |
| AC-3 | Auto-creation of scheduled from completed | [ ] |
| AC-4 | nextKmInterval → nextOdometer (backend) | [ ] |
| AC-5 | List grouping: 3 sections | [ ] |
| AC-6 | Filter status only affects scheduled | [ ] |
| AC-7 | Garage widget: two independent cards | [ ] |
| AC-8 | Detail screen: correct fields per mode | [ ] |
| AC-9 | Form validation per mode | [ ] |
| AC-10 | All strings in l10n | [ ] |
| AC-11 | dart analyze passes | [ ] |

## Phase contracts summary

| Phase | Status |
|-------|--------|
| PO | pass |
| Architect | pass |
| Design | SKIPPED (no UI design needed) |
| Backend | pass |
| Frontend | pass |
| QA | pass |
| Tech Lead | APPROVED |

## Known gaps (post-review TODOs)

1. Unit tests for `MaintenanceModel.calculateStatus()` — not written in this run
2. Unit tests for `MaintenancesCubit` grouping/filtering — not written in this run
3. Unit tests for `VehicleMaintenancesCubit.lastCompleted/nextScheduled` — not written in this run
4. `change_vehicle_mileage_bottom_sheet.dart` may be dead code — verify if any callers remain after form flow update

# Frontend Handoff — vehicle-form-specs

## Baseline Test Result
18 tests pass, 6 compilation failures (all pre-existing — caused by missing `create_maintenance_response_dto.g.dart` from prior maintenance-logic custom-iter in working tree).

## Files Changed

| File | What Changed |
|------|-------------|
| `lib/features/vehicles/domain/models/vehicle_model.dart` | Added 4 nullable String? fields (engine, horsepower, torque, weight); updated copyWith with `_unset` sentinel; updated ==, hashCode (switched to `Object.hashAll`) |
| `lib/features/vehicles/data/dto/vehicle_dto.dart` | Added 4 nullable fields to VehicleDto and toModel(); added to VehicleModelExtension.toJson() |
| `lib/features/vehicles/data/dto/vehicle_dto.g.dart` | Regenerated by build_runner |
| `lib/features/vehicles/constants/vehicle_form_fields.dart` | Added 4 string constants: engine, horsepower, torque, weight |
| `lib/features/vehicles/data/repository/vehicle_repository_impl.dart` | Added 4 spec fields to `_vehicleRequest()` map |
| `lib/features/vehicles/presentation/cubit/vehicle_form_cubit.dart` | Added mapping for 4 spec fields in `buildVehicleToSave()` |
| `lib/l10n/app_es.arb` | Added 15 new l10n keys for specs section, nav header, delete link, Placa badge, VIN optional |
| `lib/l10n/app_localizations_es.dart` | Regenerated by `flutter gen-l10n` |
| `lib/features/vehicles/presentation/form/vehicle_form_page.dart` | Full redesign: Pencil nav header, `VehicleDeleteCubit` provider, `VehicleFormBody` integration, spec fields in initial values |
| `lib/features/vehicles/presentation/form/vehicle_form_body.dart` | NEW: form body orchestrator composing all section widgets |
| `lib/features/vehicles/presentation/form/widgets/vehicle_form_nav_header.dart` | NEW: PreferredSizeWidget with Cancelar/Title/Guardar pattern |
| `lib/features/vehicles/presentation/form/widgets/vehicle_form_cover_section.dart` | NEW: cover photo section extracted |
| `lib/features/vehicles/presentation/form/widgets/vehicle_scan_banner.dart` | NEW: scan banner (UI placeholder) |
| `lib/features/vehicles/presentation/form/widgets/vehicle_form_basic_section.dart` | NEW: INFORMACIÓN BÁSICA section |
| `lib/features/vehicles/presentation/form/widgets/vehicle_form_section_header.dart` | NEW: reusable section header with optional badge and trailing |
| `lib/features/vehicles/presentation/form/widgets/vehicle_form_id_section.dart` | NEW: IDENTIFICACIÓN section with Space Mono monospace fields |
| `lib/features/vehicles/presentation/form/widgets/vehicle_form_specs_section.dart` | NEW: ESPECIFICACIONES section with 4 inline-editable rows |
| `lib/features/vehicles/presentation/form/widgets/vehicle_specs_row.dart` | NEW: StatefulWidget with tap-to-edit inline spec row |
| `lib/features/vehicles/presentation/form/widgets/vehicle_form_docs_section.dart` | NEW: DOCUMENTOS section extracted |
| `lib/features/vehicles/presentation/form/widgets/vehicle_form_cta.dart` | NEW: Guardar button + Eliminar link for edit mode |

Note: `lib/features/vehicles/presentation/widgets/vehicle_form.dart` is now an unreferenced file (no callers). It was intentionally not deleted to minimize risk — Tech Lead may flag it for removal.

## New Tests Added
No new test files added. Existing test infrastructure does not cover vehicle form widgets. The 18 previously-passing tests all still pass after this change.

## Final Test Result
- 18 passing tests (same set that passed before)
- 6 pre-existing compilation failures (unchanged from baseline)
- Command: `flutter test --no-pub test/features/profile/ test/features/users/domain/ test/features/users/presentation/cubit/ test/features/events/domain/ test/widget_test.dart`

## `dart analyze` Status
No errors in any vehicle feature file. Pre-existing errors exist in maintenance feature files (unrelated to this run).

## Manual Verification Steps
1. `flutter run -d <device>`
2. Navigate to Vehicles → "+" (add vehicle)
3. Verify nav header: "Cancelar" (left, grey) / "Agregar moto" (center, bold) / "Guardar" (right, orange)
4. Verify ESPECIFICACIONES section visible below IDENTIFICACIÓN
5. Tap a spec row (Motor, Potencia, Torque, Peso) — inline text field appears
6. Type a value, press done — value displayed in row
7. Verify Placa field shows "Obligatorio" chip in orange/accent color
8. Verify Placa input uses monospace font
9. Verify VIN field shows "Opcional" label in tertiary grey
10. Fill required fields, tap "Guardar" — vehicle saved
11. Open edit mode on saved vehicle — spec fields pre-filled
12. Open edit mode — "Eliminar vehículo" link visible at bottom
13. Tap "Eliminar vehículo" — confirmation dialog appears

## Notes for QA
- Pre-existing test failures (maintenance DTO .g.dart missing) are not regressions from this run
- `vehicle_form.dart` in `lib/features/vehicles/presentation/widgets/` is now dead code — Tech Lead will flag
- Space Mono font is loaded via `google_fonts` package (network-based) — verify on device with internet
- `FormBuilderField<String>` for read-only spec row display — QA should verify value persists correctly after inline edit

## Pre-existing Failures
- 6 test compilation failures from missing `create_maintenance_response_dto.g.dart` — existed before this run
- 1 test failure `TC-2-28: Data state shows rider email` — existed before this run
